spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/letstravel_test
    username: letstravel
    password: letstravel_test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  redis:
    host: localhost
    port: 6379
  elasticsearch:
    uris: http://localhost:9200
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: letstravel_test

app:
  jwt:
    secret: test_secret_at_least_32_characters_long_here
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000
  stripe:
    secret-key: sk_test_placeholder
    webhook-secret: whsec_placeholder
  paypal:
    client-id: placeholder
    client-secret: placeholder
    mode: sandbox
